// Backend sport identifiers. Soccer is the app-wide default sport, so its id
// is referenced from navigation, query params and fallbacks — keep it here so
// an integration change is a one-line edit.
export const SOCCER_SPORT_ID = 6046;
