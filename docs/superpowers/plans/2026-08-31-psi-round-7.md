# PSI Round 7: Banner Sizes, Blocking CSS, Hydration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Act on the 2026-08-31 deployed PSI report (FCP 2.7 s, LCP 3.6 s, TBT 300 ms, SI 3.8 s): fix the banner rendition selection (−46 KiB on the LCP image path), shrink the 600 ms render-blocking stylesheet, and cut hydration long-tasks on the mobile home fold.

**Architecture:** Task 1 corrects `TheBanner.vue`'s `sizes` default so mobile resolves to the real slide width (~611 device px → the existing 640w rendition) instead of `100vw` (→ 960w). Task 2 measures what actually fills the 303 KB raw / 38 KB gzip `entry.css`, then either evicts route-only CSS from the entry merge or inlines the critical subset — decision by measurement against an explicit gate. Task 3 audits which mobile-home components hydrate eagerly below the fold, extends Nuxt lazy-hydration to them, and attributes the 177 ms forced reflow via a sourcemapped build. Task 4 re-measures and records results.

**Tech Stack:** Nuxt 4.5 SSR, Vite CSS code-splitting + the existing `build:manifest` hook in `nuxt.config.js`, Nuxt lazy hydration (`hydrate-on-visible` / `hydrate-on-idle`), Lighthouse 13 local harness.

**Spec:** the in-chat design approved 2026-08-31 ("proceed") — pasted PSI report is the driving evidence; this file is the written record.

## Global Constraints

- JavaScript only — no TypeScript.
- No dependency changes in this round (no new packages, none removed). If Task 2 concludes critical-inline tooling (e.g. `beasties`) is the only viable path, STOP that task and report back — adding a devDependency is a controller decision, not the implementer's.
- Semantic Tailwind tokens only in any new/edited styles; no literal colours below `app/assets/css/style.css` layer 2.
- Do not regress prior rounds' invariants: exactly ≤2 render-blocking stylesheets on the home page; critical JS (modulepreload set) must not grow; CLS stays ≤0.01 locally; the LCP banner preload (`imagesrcset`/`imagesizes`) must stay consistent with the `<source>` markup.
- Do not edit `Dockerfile`, CI files, `docs/INFRA-HANDOFF.md`, or `.env`; never commit `.env` or `.vercel/`.
- `.env` uses `KEY= value` — load with a `while read` loop, never `source`.
- Local Lighthouse: `npx lighthouse` with `npm_config_cache` in the session scratchpad, `--throttling-method=devtools`, against the built server on port 3131 (kill it afterwards). Never leave a server running at task end.
- Commits: conventional `type: summary`, ending `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Do not push.
- `pnpm build` must succeed after every task; eslint on changed files adds no errors beyond the pre-existing Nuxt-auto-import `no-undef` class.

---

### Task 1: Banner `sizes` — stop shipping 960w to phones

**Files:**
- Modify: `app/components/TheBanner.vue:27-30` (the `sizes` prop default)
- Modify: `app/components/DesktopSportsLayout.vue` / `app/components/MobileSportsLayout.vue` ONLY IF they pass an explicit `sizes` prop to `<LazyTheBanner>` (check first; if they don't, leave them).

**Interfaces:**
- Produces: nothing consumed by later tasks.

Background: `.banner-slide` is `width: calc((100% - 0.75rem) / 1.1)` under 1024px, inside a page container with `px-2` padding (0.5rem × 2 = 1rem total; verify the actual mobile wrapper's horizontal padding by reading `app/layouts/default.vue` small-screen branch and `MobileSportsLayout.vue` before finalizing the calc — if padding differs from 1rem, adjust the subtracted constant to match and note it in the report). On ≥1024px the slide is 100% of the existing `min(1000px, calc(100vw - 664px))` container, which the current default already handles.

- [ ] **Step 1: Change the default `sizes` prop**

In `app/components/TheBanner.vue`, replace:

```js
const props = defineProps({
  sizes: {
    type: String,
    default: "(min-width: 1024px) min(1000px, calc(100vw - 664px)), 100vw",
  },
});
```

with (adjusting `1.75rem` = container padding 1rem + gap 0.75rem if the measured padding differs):

```js
const props = defineProps({
  // Mobile: the visible slide is (container − 0.75rem gap) / 1.1 wide
  // (see .banner-slide below) inside a 1rem-padded page — telling the
  // browser 100vw made it fetch the 960w rendition on ~400px phones;
  // the true ~85vw resolves to the 640w file (−46 KiB on the LCP path,
  // per the 2026-08-31 PSI report).
  sizes: {
    type: String,
    default:
      "(min-width: 1024px) min(1000px, calc(100vw - 664px)), calc((100vw - 1.75rem) / 1.1)",
  },
});
```

- [ ] **Step 2: Verify the preload matches**

The `useHead` preload uses `imagesizes: props.sizes` — confirm no other literal `100vw` remains in the file (`grep -n "100vw" app/components/TheBanner.vue` → only the new calc line).

- [ ] **Step 3: Build and verify rendition selection**

Run: `pnpm build 2>&1 | tail -3` → success.
Run: `PORT=3131 node .output/server/index.mjs & sleep 4; curl -s http://localhost:3131/ | grep -o 'imagesizes="[^"]*"' | head -2; curl -s http://localhost:3131/ | grep -o 'sizes="[^"]*"' | head -3; kill %1` → every occurrence shows the new calc, none show bare `100vw`.
Arithmetic check (record in the report): at 412 CSS px viewport, DPR 1.75 → `(412 − 28) / 1.1 × 1.75 ≈ 611` device px → the browser's srcset picker (640 ≥ 611) selects `…-640.webp`, not 960.

- [ ] **Step 4: Commit**

```bash
git add app/components/TheBanner.vue
git commit -m "perf: correct banner sizes so phones fetch the 640w rendition

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Shrink the render-blocking stylesheet (measure → decide → implement)

**Files:**
- Modify: `nuxt.config.js` (the `build:manifest` hook and/or `vite.build` block, depending on the decision)
- Possibly modify: `app/assets/css/style.css` (only to remove verifiably dead rules found by the measurement — never design-system layers 1–3)
- Possibly modify: component `<style>` blocks or static-import graphs identified as entry-CSS contributors (list them in the report)

**Interfaces:**
- Consumes: nothing from Task 1.
- Produces: the home page's render-blocking CSS budget for Task 4 to verify.

Current state: `entry.CfbhoblA.css` is 302,878 bytes raw / ~38 KB gzip and is the only stylesheet — every component whose CSS lands in the entry graph ships render-blocking on every page. PSI attributes 600 ms of Slow-4G blocking to it.

- [ ] **Step 1: Attribute the bytes**

Build with a manifest inspection: after `pnpm build`, use Vite's client manifest (`.output/server/chunks/` is not needed — read the client manifest JSON under `node_modules/.cache/nuxt/.nuxt/dist/client/_nuxt/` or `.output/public/_nuxt/` + the `builds` manifest, whichever exists; the `build:manifest` hook in `nuxt.config.js` already shows how chunk metadata is accessed) to list which modules' CSS was concatenated into the entry stylesheet. Cross-check by searching `entry.*.css` for distinctive class strings from candidate components (e.g. scoped `data-v-` hashes mapped back via `grep -rl "<style" app/components app/pages | xargs grep -l scoped`, then matching each file's compiled hash found in `.output`). Produce a table in the report: contributor → approx bytes → rendered on mobile home above the fold? (yes/no).

- [ ] **Step 2: Decide against the gate**

Gate: home-page render-blocking CSS ≤ 25 KB gzip with ≤2 stylesheets, CLS unchanged, no flash-of-unstyled-content on any of `/`, `/share-bets`, `/join-affiliate`, `/casino-home`.
- If ≥ ~30% of entry CSS belongs to components/routes NOT rendered on the home/sports path → implement **eviction**: stop force-merging those chunks' CSS into the entry (adjust the `build:manifest` hook's stripping rules and/or convert the responsible static imports to dynamic/`Lazy` so their CSS rides their own chunk, loaded on navigation). Their CSS must still load on the pages that need it — verify each affected route visually renders styled.
- Else (the CSS really is mostly global) → implement **defer-noncritical**: keep `app/assets/css/style.css` render-blocking but move the largest non-critical, self-contained blocks (e.g. festive/animations/rare-page utilities identified in Step 1) into a second stylesheet loaded with `media="print"` + `onload="this.media='all'"` and a `<noscript>` fallback via `app.head`. Hand-curate ONLY whole self-contained blocks, never token layers.
- If neither path can reach the gate without new tooling, STOP and report `BLOCKED: needs critical-CSS tooling decision`.

- [ ] **Step 3: Implement the chosen path, verify**

`pnpm build`; `ls .output/public/_nuxt/*.css | wc -l`; gzip-measure the render-blocking set referenced by the home HTML (`curl` the built server's `/` and list `<link rel="stylesheet">` hrefs; gzip each). Then serve and eyeball (headless screenshot or Chrome MCP) `/`, `/share-bets`, `/join-affiliate`, `/casino-home` for unstyled flashes. Run `node scripts/critical-bytes.mjs` and record the line. Kill servers.

- [ ] **Step 4: Commit**

```bash
git add -A -- nuxt.config.js app/assets/css/style.css app/components app/pages
git commit -m "perf: cut render-blocking CSS on the home path

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

(Adjust the pathspec to what actually changed; commit message may name the chosen strategy.)

---

### Task 3: Mobile-home hydration audit — lazy-hydrate below-fold, attribute the forced reflow

**Files:**
- Modify: `app/components/MobileSportsLayout.vue` (and `DesktopSportsLayout.vue` where the same components appear below the fold)
- Possibly modify: the components identified (wrap usage as `<LazyX hydrate-on-visible>`, no internal rewrites)
- Modify: `nuxt.config.js` ONLY if a temporary `sourcemap: true` toggle is needed for attribution (revert before committing)

**Interfaces:**
- Consumes: nothing from A/B.
- Produces: the eager-hydration surface for Task 4's TBT comparison.

- [ ] **Step 1: Enumerate the fold**

Read `MobileSportsLayout.vue` top-to-bottom and classify every child: above-fold on a 412×915 viewport (header, odds bar, banner, filters, first match cards) vs below-fold (hot-games strips, promo strips, community/SEO blocks, anything after the first screen). List the classification in the report. Already-lazy components (`LazyTheBanner hydrate-on-idle`, `LazySEOMarkupContent`, `LazyFooter`) are done — skip.

- [ ] **Step 2: Extend lazy hydration**

For each below-fold, non-interactive-at-load component: switch its usage to `<LazyX hydrate-on-visible>` (Nuxt auto-generates the `Lazy` prefix; keep props/slots identical). For components that must respond to store changes immediately after load, prefer `hydrate-on-idle`. Do NOT lazy-hydrate anything that owns above-fold layout (CLS risk) or the match list itself. SSR markup must not change (lazy hydration keeps SSR output; verify no hydration-mismatch warnings in dev console).

- [ ] **Step 3: Attribute the forced reflow**

Build once with client sourcemaps (`vite.build.sourcemap: true` temporarily), reproduce locally: `npx lighthouse http://localhost:3131/ --throttling-method=devtools …` and open the trace's forced-reflow attribution, or use the built map to resolve the deployed offsets (`BWrfnNKR.js:3:2100`, `DjL6aZh1.js:2:18372`) with `node -e` + `source-map`-free manual lookup (`npx source-map-cli` is not available — use the `.map` JSON's `mappings` via a small script, or simply rebuild and re-run Lighthouse locally and read the new attribution, which will name local chunk files whose maps exist). Identify the app-code caller (suspects: `useFlyToBetslip`, match-list measurement, `useScreenSizes`). If it is app code reading layout in a loop → batch the reads (single `getBoundingClientRect` cached per frame) as part of this task; if it is framework/library internals → document and park. Revert the sourcemap toggle.

- [ ] **Step 4: Verify**

`pnpm build`; serve; local Lighthouse (devtools throttling) on `/` — record TBT and the long-task list vs the baseline (TBT 300 ms, tasks 369/144 ms deployed). Confirm no hydration warnings and that lazy-hydrated sections become interactive on scroll. Kill servers.

- [ ] **Step 5: Commit**

```bash
git add app/components nuxt.config.js
git commit -m "perf: lazy-hydrate below-fold home sections, fix forced reflow

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Measure and record

**Files:**
- Modify: `docs/superpowers/plans/2026-08-31-psi-round-7.md` (append `## Results`)

- [ ] **Step 1:** Full build; `node scripts/critical-bytes.mjs`; local Lighthouse ×2 on `/` with `--throttling-method=devtools` and the `.env`-loaded live-data setup if available (else note conditions); record FCP/LCP/TBT/CLS/SI + perf score, render-blocking CSS bytes/count, and the banner rendition actually requested (from the Lighthouse network items: must be `…-640.webp` on the emulated phone).
- [ ] **Step 2:** Append `## Results` with before (PSI 2026-08-31: FCP 2.7 s, LCP 3.6 s, TBT 300 ms, SI 3.8 s, blocking CSS 37.4 KiB/600 ms) vs after (local), per-task outcomes, the Task 2 contributor table summary, and anything parked.
- [ ] **Step 3:** Commit:

```bash
git add docs/superpowers/plans/2026-08-31-psi-round-7.md
git commit -m "docs: results for PSI round 7

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Results

### Measurement conditions (read this before comparing numbers)

Local Lighthouse (13.4.1), `--throttling-method=devtools --only-categories=performance`,
headless Chrome, against `node .output/server/index.mjs` on port 3131, `.env` loaded via
the required `while read` loop (never `source`). **Live backend APIs (matches/casino/etc.)
were CORS-blocked from this local server**, exactly as in Task 3 — the home page's match
list resolves to "No matches found" and the below-fold `GamesRow` strip never receives
data, so it never hydrates locally (its lazy-hydration deferral is real per the diff but
unexercised by these runs — see Task 3's parked item). This also means local numbers are
**not directly comparable to the deployed PSI run**, which had live data, a CDN, and no
local-machine/headless-Chrome noise. Comparability is between the two local runs and
against Task 3's own local baseline numbers, not an apples-to-apples claim against the
2026-08-31 PSI report. Local run-to-run TBT variance is large (as Task 3 already found)
— both runs below are reported, not cherry-picked.

### Lighthouse ×2 (local, `/`, devtools throttling)

| Metric | Run 1 | Run 2 |
|---|---|---|
| Performance score | 0.81 | 0.77 |
| FCP | 2.24 s | 2.24 s |
| LCP | 2.24 s | 2.24 s |
| TBT | 531 ms | 791 ms |
| CLS | 0.031 | 0.031 |
| Speed Index | 3.47 s | 3.10 s |
| Render-blocking stylesheet | `entry.BCjVBijJ.css`, 26,555 B transfer (brotli), 1 file | same |
| LCP request | `/banners/banda/starter-free-bet-**640**.webp` | `/banners/banda/starter-free-bet-**640**.webp` |

**LCP banner proof point (this round's hard requirement): confirmed.** Both runs'
`network-requests` audit shows the LCP `<img>` requesting `starter-free-bet-640.webp`,
not `-960.webp`, on the emulated mobile viewport — Task 1's `sizes` fix is working
end-to-end from markup to actual browser rendition selection.

**FCP == LCP in both runs** because with no live match data the banner image is the
first and only meaningful paint on the page locally (no match cards render) — a local-
data artifact, not a metric bug.

**CLS 0.031** (above the plan's ≤0.01 invariant) is attributable to the "No matches
found" empty-state block (`div.sports-matches`, score 0.031 per `layout-shifts` audit
details) and a small font-swap shift in the sport-tabs row — both are consequences of
the CORS-blocked local data path (the real deployed page renders match cards
immediately, no empty-state swap), not a regression introduced by Tasks 1–3. Flagged
for a live/staging re-check rather than treated as a local finding against the
invariant.

**Render-blocking transfer size note:** Lighthouse's `network-requests`/
`render-blocking-insight` audits report 26,555 B for `entry.*.css` because Chrome
negotiated `Content-Encoding: br` (nitro serves precompressed Brotli when the client
accepts it) — smaller than the 34,151 B gzip figure Task 2 measured from nitro's `.gz`
sidecar. Both are the same file; the difference is compression algorithm, not a second
measurement disagreeing with the first. `scripts/critical-bytes.mjs` (below) uses the
`.gz` sidecar deliberately for a stable, encoding-independent number.

### `node scripts/critical-bytes.mjs http://localhost:3131/`

```
modulepreload=67 js_gzip=219KB stylesheets=1 css_gzip=33KB html_gzip=39KB
```

`stylesheets=1` confirms only `entry.*.css` counts as render-blocking (`noncritical.css`
correctly excluded — `media="print"` swap). `css_gzip=33KB` matches Task 2's final
34.1 KB / 33.4 KB gzip figure (rounding).

### Before (deployed PSI, 2026-08-31) vs after (local)

| Metric | Before (deployed PSI) | After (local, this round) | Notes |
|---|---|---|---|
| FCP | 2.7 s | 2.24 s | Not apples-to-apples — see conditions above |
| LCP | 3.6 s | 2.24 s | LCP image now provably 640w, not 960w, on phones |
| TBT | 300 ms | 531 ms / 791 ms | Local noise; Task 3's own local A/B was also inconclusive on TBT alone |
| Speed Index | 3.8 s | 3.10 s / 3.47 s | |
| Render-blocking CSS | 37.4 KiB / 600 ms | 33.4 KB gzip / 1 stylesheet (Task 2 final number) | −3.3 KB gzip / −8.2%; gate (≤25 KB) not reached |
| LCP banner rendition | 960w (per PSI evidence driving this round) | **640w, confirmed via network items** | Task 1's fix verified end-to-end |

The deployed-vs-local gap (LCP/FCP/SI all reading better locally, TBT reading worse) is
dominated by measurement conditions, not a claim that the app got 1.4 s faster in
production — the only production-grade, condition-independent proof points from this
round are: the LCP image is provably the 640w file (was 960w), and render-blocking CSS
dropped by a measured, reproducible 3.3 KB gzip with the blocking-stylesheet count held
at 1.

### Per-task outcomes

**Task 1 — banner `sizes`.** `TheBanner.vue`'s `sizes` default changed from bare `100vw`
to `calc((100vw - 1.75rem) / 1.1)` on mobile. Verified twice: statically (412 CSS px ×
1.75 DPR → ~611 device px → picks the 640w candidate) and now dynamically, via this
task's Lighthouse network-requests capture showing the actual browser-selected LCP
request is `starter-free-bet-640.webp`. Commit `1397c00`.

**Task 2 — render-blocking CSS.** Attributed `entry.css`'s 302,878 raw bytes: ~82%
Tailwind's whole-app-scanned utility layer (not chunk- or route-attributable without new
critical-CSS tooling), ~4% design-system boilerplate (untouched), ~2% globally-needed
scoped chrome components, remainder hand-written decorative/animation CSS. Decision:
defer-noncritical (matches the brief's decision tree — the CSS is architecturally global,
not a component-eviction problem). Implemented: moved verified-safe, verified-unused-
on-the-4-gated-routes CSS to `public/css/noncritical.css` (`media="print"` → `onload`
swap, `<noscript>` fallback, content-hash-cache-busted `?v=` query string). A review
round caught and fixed a structural regression (`.app-carousel-track`/`.app-carousel-
slide` moved back to render-blocking after being found to cause a real FOUC on
`/share-bets` and `/join-affiliate`). Final: render-blocking CSS 36.4 KB → **33.4 KB
gzip (−8.2%)**, exactly **1** render-blocking stylesheet (gate is ≤2). The ≤25 KB gate
itself was **not met** — see parked item (a) below. Commits `e418cc1`, `b6ade26`.

**Task 3 — lazy hydration + forced reflow.** Below-fold mobile-home sections
(`MobileGamesRow`/remaining-categories strip) switched to
`<LazyMobileGamesRow hydrate-on-visible>` in both `MobileTest.vue` and
`TheLanding.vue` (a review round caught that the naive `<LazyGamesRow>` tag resolved to
no component at all — Nuxt's directory-based auto-naming requires `MobileGamesRow`).
The `useScrollToSelectedSport` forced reflow (`DjL6aZh1.js:2:18372`) was fixed: the
broken `alreadyVisible` guard (always comparing against an unclipped immediate parent,
so it always read `true` and permanently skipped `scrollIntoView()`) was replaced with a
`findScrollableAncestor()` walk plus one batched `getBoundingClientRect()` read;
reflow time at that call site dropped ~31% (60.3 ms → ~41.4 ms) across repeated local
runs. The other attributed reflow site (`BWrfnNKR.js:3:2100`, Nuxt's own
`hydrate-on-visible` viewport check) is framework internals — parked, not app code.
Commits `96bccd0`, `ff4f0e8`.

### Task 2 contributor table (summary, full detail in `task-2-report.md`)

| Contributor | Raw bytes | % of file | Above the fold on home? |
|---|---|---|---|
| Tailwind `@layer properties` (v4 reset boilerplate) | 2,418 | 0.8% | Yes |
| Tailwind `@layer theme` (design tokens) | 5,913 | 2.0% | Yes — layer 2/3, never touched |
| Tailwind `@layer base` (element resets) | 4,220 | 1.4% | Yes |
| Tailwind `@layer utilities` (whole-app-scanned utility classes) | ~247,600 | 81.8% | Mixed — not separable per-route without a rearchitecture |
| 7 scoped chrome components (`data-v-*`) | 5,884 | 1.9% | Yes — global chrome |
| Hand-written decorative/animation classes (moved to `noncritical.css`, minus the carousel carve-out) | 11,363 | 3.8% | No on the 4 gated routes |
| Unattributed (nested `@media` parsing overhead) | ~25,500 | 8.4% | Mixed, same character as utilities |

### Parked items

**(a) The ≤25 KB gzip render-blocking CSS gate was not reached.** Root cause: Tailwind
v4's `@import "tailwindcss"` scans the whole `app/` tree and emits one flat utility file
(~82% of `entry.css`) with no per-chunk or per-route boundary — neither of Task 2's
prescribed levers (chunk eviction via the `build:manifest` hook, or hand-curated
`media="print"` deferral of whole self-contained blocks) can touch compiler-generated
utility output. Closing the remaining ~8 KB gzip gap needs an actual tooling decision:
either genuine critical-CSS extraction (Beasties/Critters-style, DOM-aware pruning — a
new dependency, out of scope for this round's "no dependency changes" constraint) or
splitting Tailwind's own `@source` scanning into multiple route-scoped CSS entries
(native capability, no new dependency, but a real re-architecture with FOUC risk if any
home-rendered class is missed) — needs a controller decision before the next round
attempts it.

**(b) Residual forced-reflow from Nuxt's `hydrate-on-visible` internals.** The
`BWrfnNKR.js:3:2100` attribution point is inside Nuxt's own lazy-hydration viewport-
intersection bookkeeping, not app code — nothing in this repo can change it without
patching the framework. Documented, not actioned.

**(c) `GamesRow` lazy-hydration benefit not exercisable locally.** The `MobileGamesRow`/
`LazyMobileGamesRow hydrate-on-visible` change from Task 3 is structurally correct
(verified fold position, no SSR/CLS/hydration-mismatch risk) but its backing data
(`categoriesWithGames`) never resolves against the CORS-blocked local API, in either this
round's runs or Task 3's own — so its actual TBT/long-task impact has never been measured
locally. Needs a live/staging check to confirm the deferred strip renders and becomes
interactive on scroll with real casino data.
